# 클로드로 이 작업 하는 방법 — 원모용 정리

> 헷갈리는 게 당연하다. **클로드가 도는 자리가 두 군데**여서 그렇다.
> 이 문서는 그 둘을 구분하는 것부터 시작한다.

---

## 1. 지금 뭐가 어디서 돌고 있나

지금 원모가 쓰고 있는 건 **웹 클로드 코드**다. 브라우저(claude.ai/code)에서 시킨다.

| | **웹 클로드 코드** (지금 이거) | **데스크톱/터미널 클로드 코드** |
|---|---|---|
| 어디서 도나 | **인터넷 저편의 임시 컴퓨터** | **원모 컴퓨터 안** |
| 내 D: 드라이브 | **못 본다.** 아예 연결이 없다 | **본다.** `D:\클로드` 그냥 쓴다 |
| 파일이 남나 | **안 남는다.** 세션 끝나면 컴퓨터가 사라진다 | 남는다 |
| 그럼 뭐가 남나 | **깃허브에 올린 것만 남는다** | 컴퓨터에 그대로 |
| 좋은 점 | 폰으로도 시킬 수 있다. 설치가 필요 없다 | 내 파일 직접 만진다 |

### 그래서 지금 안 되는 것 세 가지

원모가 부탁한 것 중에 **이 창에서는 원래 불가능한** 것들이다. 내가 게을러서가 아니다.

1. **`D:\클로드` 폴더에 저장** — 이 세션은 원모 컴퓨터에 접근할 방법이 없다
2. **파이썬 깔려 있는지 확인** — 확인할 수 있는 건 *저편 임시 컴퓨터*의 파이썬이다
   (참고로 저쪽에는 깔려 있다. `python3 story/script/build-webtoon.py`가 여기서 돈다)
3. **작업 폴더를 D:로 변경** — 바꿀 폴더가 애초에 원모 컴퓨터에 없다

**→ 이걸 하려면 아래 3번 「내 컴퓨터에 내려받기」로 간다.**

---

## 2. 그래도 남는다 — 저장은 이미 되고 있다

임시 컴퓨터는 사라지지만 **깃허브 저장소는 안 사라진다.**

```
github.com/wonmo2000-art/flmo
브랜치: claude/hibakusha-film-chapter-one-ssvbt2
```

내가 작업 끝날 때마다 여기 올린다(커밋·푸시). 그래서 원모가 할 일은 없다.
**새 세션을 시작해도 내가 저장소를 읽고 이어서 한다.**

> 이게 `CLAUDE.md`에 *"작업이 끝나면 반드시 커밋하고 푸시한다"*라고
> 적혀 있는 이유다. 그게 이 프로젝트의 저장 버튼이다.

---

## 3. 내 컴퓨터(D: 드라이브)에 내려받기

두 가지 방법이 있다. **위쪽이 훨씬 쉽다.**

### 방법 A — 압축 파일 받기 (제일 쉬움)

대화창에 내가 올려주는 `.zip` 파일을 **다운로드해서 `D:\클로드`에 풀면 끝.**
새 원고가 나올 때마다 *"압축해서 보내줘"* 하면 된다.

### 방법 B — 깃허브에서 직접 받기 (매번 최신)

1. 브라우저에서 → `github.com/wonmo2000-art/flmo`
2. 왼쪽 위 브랜치 목록에서 **`claude/hibakusha-film-chapter-one-ssvbt2`** 선택
3. 초록색 **Code** 버튼 → **Download ZIP**
4. `D:\클로드`에 풀기

> 매번 손으로 받는 게 귀찮아지면 그때 데스크톱 클로드 코드를 깔면 된다.
> 그러면 `D:\클로드`에서 바로 작업하고, 내가 그 폴더를 직접 만진다.
> **급하지 않다. 지금 방식으로도 원고는 계속 쌓인다.**

---

## 4. 나한테 시키는 법 — 이 세 마디만 알면 된다

### ① 아이디어가 떠올랐을 때

> **"인박스에 적어줘: (아무 말이나)"**

문장이 안 되어도, 오타가 있어도, 나중에 버릴 것 같아도 그냥 말하면 된다.
`story/inbox.md`에 **원문 그대로** 남기고 그다음에 정리한다.
좋은 아이디어가 정리하다가 사라지는 게 제일 큰 손실이라서 이 순서다.

### ② 지금 어디까지 왔는지 모를 때

> **"지금 상태 정리해줘"**

`story/README.md`를 보면 된다. 그 문서 하나에 전부 들어 있다.
새 세션에서 내가 제일 먼저 읽는 문서이기도 하다.

### ③ 원고를 고치고 싶을 때

> **"1화 D파트 다시 써줘. (뭐가 마음에 안 드는지)"**

파트 이름(A~M)이나 컷 번호(D24, M51)로 짚어주면 정확하다.
*"거기 그거"* 라고 해도 대개 찾는다.

---

## 5. 세션이 새로 시작됐을 때 (컨테이너가 바뀌었을 때)

**원모가 할 일은 없다.** 그냥 하고 싶은 말을 하면 된다.

내가 알아서 `CLAUDE.md` → `story/README.md` → `story/script/ep01.md` 순서로 읽고
어디까지 왔는지 파악한 다음 이어서 한다.

단, **이전 대화 내용은 안 남는다.** 저장소에 쓰인 것만 남는다.
그래서 중요한 이야기는 내가 그때그때 파일로 박아둔다.

> 혹시 내가 뭘 모르는 것 같으면 *"저장소 먼저 읽어"* 라고 하면 된다.

---

## 6. 파일이 어디 뭐가 있나

```
HOW-TO.md          ← 지금 이 문서
CLAUDE.md          클로드에게 주는 작업 지침

story/
  README.md        ★ 지금 어디까지 왔나. 헷갈리면 여기부터
  inbox.md         날것 아이디어. 아무렇게나 던져놓는 곳
  premise.md       로그라인 · 주제 · 톤
  world.md         세계관
  outline.md       연표 · 계급 · 3막 · 남은 질문
  characters.md    인물
  visual.md        비주얼 규칙
  script/
    ep01.md            ★ 1화 원고 (본체)
    ep01-webtoon.html  1화를 웹툰처럼 읽는 페이지
    build-webtoon.py   위 HTML을 굽는 스크립트
    webtoon.css        그 페이지 스타일
  prompts/         이미지·영상 생성 프롬프트

research/
  notes.md         자료조사
  sources.md       출처
```

### 웹툰 페이지 다시 굽는 법

`ep01.md`를 고치면 HTML도 다시 만들어야 한다. 내가 알아서 하지만, 직접 하려면:

```
python3 story/script/build-webtoon.py
```

*(원모 컴퓨터에서 하려면 파이썬 3이 필요하다. 없으면 그냥 나한테 시키면 된다.)*

---

## 7. 작업 원칙 — 이건 안 바뀐다

**사실관계는 지어내지 않는다.** 피폭은 실제로 사람들이 겪은 일이다.
극적 각색은 하되, 역사적 사실로 제시하는 부분은 출처를 `research/sources.md`에 남긴다.
확인 못 한 내용은 확인 못 했다고 표시한다.

그리고 **내가 임의로 정한 건 항상 표시해둔다.**
`story/inbox.md` 맨 아래 *"이번에 내가 정한 것"* 항목이 그거다.
마음에 안 들면 그 줄 짚어서 말하면 된다.
